name: Playwright Tests (Docker)

on:
  push:
    branches:
      - main
  schedule:
    # Run every Sunday at 00:00 UTC
    - cron: '0 0 * * 0'
  workflow_dispatch:

jobs:
  test-docker:
    timeout-minutes: 60
    runs-on: blacksmith-8vcpu-ubuntu-2404

    steps:
    - uses: actions/checkout@v4

    - name: Build Docker image
      run: docker build -t chroma-test .

    - name: Run Playwright tests
      run: |
        docker run --rm --shm-size=2gb \
          -v ${{ github.workspace }}/packages/e2e-polkadot-js/playwright-report:/app/packages/e2e-polkadot-js/playwright-report \
          -v ${{ github.workspace }}/packages/e2e-polkadot-js/test-results:/app/packages/e2e-polkadot-js/test-results \
          chroma-test

    - uses: actions/upload-artifact@v4
      if: ${{ !cancelled() }}
      with:
        name: playwright-report-docker
        path: packages/e2e-polkadot-js/playwright-report/
        retention-days: 30

    - uses: actions/upload-artifact@v4
      if: failure()
      with:
        name: playwright-test-results-docker
        path: packages/e2e-polkadot-js/test-results/
        retention-days: 30
